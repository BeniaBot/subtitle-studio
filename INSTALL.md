# התקנה והסרה · אולפן הכתוביות

התוכנה רצה **גם בלי להתקין** — מספיק להוריד את `SubtitleStudio.exe`
וללחוץ פעמיים. ההתקנה נועדה רק למי שרוצה קיצור בתפריט התחל
ושורה ברשימת התוכנות של ווינדוס.

---

## התקנה רגילה

מריצים את `SubtitleStudio-Setup.exe` ולוחצים ״התקנה״.

- **בלי הרשאות מנהל.** ההתקנה היא למשתמש הנוכחי בלבד.
- ברירת המחדל: `%LOCALAPPDATA%\Programs\SubtitleStudio`
- נוצר קיצור בתפריט התחל, ואם סימנתם — גם בשולחן העבודה.
- התוכנה מופיעה ב״הוספה או הסרה של תוכניות״.

הכול עובד גם במקלדת: ‏`Tab` בין הפקדים, ‏`Enter` להתקנה, ‏`Esc` לביטול.

---

## התקנה שקטה

```
SubtitleStudio-Setup.exe /S
SubtitleStudio-Setup.exe /S /D="C:\Tools\SubtitleStudio"
```

| דגל | מה הוא עושה |
|---|---|
| `/S` | בלי ממשק. גם **מכבה** את קיצור שולחן העבודה |
| `/D="נתיב"` | לאן להתקין (‏במקום ברירת המחדל) |
| `/desktop` · `/nodesktop` | להוסיף/לא להוסיף קיצור בשולחן העבודה |
| `/run` | להפעיל את התוכנה בסיום |
| `/waitpid=N` | להמתין עד 30 שניות שתהליך מסוים ייסגר לפני ההחלפה |
| `/cleanself` | למחוק את קובץ ההתקנה עצמו באתחול הבא |

קודי יציאה: `0` הצלחה · `1` שגיאה · `2` ביטול.
יומן מפורט: `%TEMP%\substudio-setup.log`.

---

## הסרה

שלוש דרכים, כולן שקולות:

```
uninstall.exe                       (בתיקיית ההתקנה)
uninstall.exe /S                    (שקט)
SubtitleStudio-Setup.exe /uninstall
```

או פשוט ״הוספה או הסרה של תוכניות״ בהגדרות ווינדוס.

**מה נמחק:** התוכנה, הקיצורים, והרישום בווינדוס.

**מה נשאר:**

- `%APPDATA%\SubtitleStudio\settings.ini` — ההעדפות שלכם.
  התקנה מחדש תמצא אותן במקום.
- קובצי הכתוביות והסרטים שיצרתם. המתקין לא יודע עליהם ולא מחפש אותם.
- `%LOCALAPPDATA%\SubtitleStudio\runtime` — מנוע הווידאו (‏כ-‏100MB).
  חלון ההסרה מציע למחוק גם אותו, בתיבת סימון שאינה מסומנת מראש.
  ההסרה השקטה **לא** נוגעת בו.

אם נשארו בתיקייה קבצים שההתקנה לא יצרה, התיקייה לא נמחקת ומופיעה
הודעה שמסבירה את זה. עדיף מאשר למחוק למישהו קובץ ששם שם בעצמו.

---

## מה בונה את מה

```
build.cmd                  ->  dist\SubtitleStudio.exe        (~36MB, הכול בפנים)
build\make-installer.cmd   ->  dist\SubtitleStudio-Setup.exe  (~37MB)
```

**סדר הפעולות חשוב.** המתקין מטמיע את `dist\SubtitleStudio.exe` כמשאב,
אז בנייה שלו לפני `build.cmd` תארוז את הקובץ הקודם — בשקט, בלי אזהרה.

המתקין נבנה עם אותו מהדר מובנה של ווינדוס
(`%WINDIR%\Microsoft.NET\Framework64\v4.0.30319\csc.exe`), בלי SDK,
בלי NuGet ובלי כלי התקנה חיצוני. הוא עצמו תוכנת WinForms רגילה,
והמקור שלו ב-`src\Installer\`.

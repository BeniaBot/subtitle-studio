# אולפן הכתוביות — מסמך עבודה

**קראו את הקובץ הזה לפני כל שינוי בפרויקט.** הוא מתעד את הארכיטקטורה, המוסכמות,
המלכודות שכבר נפלנו בהן, ואת מצב העבודה הנוכחי. כל סשן שמשנה משהו — מעדכן כאן מיד.

---

## 1. מה זה

תוכנת דסקטופ לווינדוס ליצירה, תיקון והטמעה של כתוביות בעברית.
**קובץ EXE יחיד, בלי התקנה, בלי אינטרנט, בלי ספריות חיצוניות.**

- קוד: `src\*.cs` (C#, WinForms)
- פלט: `dist\SubtitleStudio.exe` (~38MB — הכל בפנים)
- בנייה: `build.cmd`
- גרסה נוכחית: `App.Version` ב-`src\Updater.cs` (כרגע `0.4.0`)

---

## 2. בנייה

```
build.cmd
```

1. יוצר `build\app.ico` אם חסר (`build\make-icon.ps1`, מצייר את האייקון בקוד).
2. אורז את `tools\ffmpeg.exe` ל-`build\payload\ffmpeg.pack` (‏102MB → 36MB, Deflate).
   הפעולה מדלגת אם החבילה עדכנית. למחוק את הקובץ כדי לאלץ אריזה מחדש.
3. מהדר עם **המהדר המובנה בווינדוס**:
   `%WINDIR%\Microsoft.NET\Framework64\v4.0.30319\csc.exe`
   ומטמיע את `ffmpeg.pack` כמשאב.

### אילוצים קריטיים בבנייה

| אילוץ | פירוט |
|---|---|
| **C# 5 בלבד** | המהדר הזה הוא מ-.NET Framework. **אין** אינטרפולציית מחרוזות (`$"..."`), `?.`, `nameof`, מאתחלי מאפיינים, `out var`, פונקציות מקומיות. יש `async`, למדות ו-`delegate`. |
| **‎.NET Framework 4.8** | מובנה בווינדוס 10/11. אין SDK, אין NuGet, אין חבילות. |
| **`/codepage:65001`** | קבצי המקור הם UTF-8 בלי BOM והמהדר חייב את הדגל הזה כדי לקרוא עברית נכון. |
| **`build.cmd` חייב להיות ASCII** | cmd עוקב אחרי היסטי בייטים; תו עברי אחד אפילו בהערה שובר את הריצה. ההודעות בו באנגלית בכוונה. |
| **לסגור את התוכנה לפני בנייה** | אחרת `dist\SubtitleStudio.exe` נעול והקומפילציה נכשלת עם `CS0016`. |

---

## 3. כלי הבדיקה (`build\*.ps1`)

| סקריפט | מה הוא עושה |
|---|---|
| `test-run.ps1 -Wait N -Shot x.png [-Media path]` | בונה, מפעיל, מצלם את החלון (‏PrintWindow — עובד גם כשהחלון מוסתר) |
| `poke.ps1 -Keys "f5,esc"` | שולח מקשים בודדים ב-PostMessage (בלי מודיפיירים) ומצלם |
| `poke3.ps1 -Key "T"` | שולח **Ctrl+אות** ב-keybd_event אמיתי (PostMessage לא עובד למודיפיירים) |
| `click.ps1 -X -Y` | לחיצת עכבר אמיתית בקואורדינטות לקוח, ומצלם |
| `dialog-gallery.ps1` | טוען את ה-EXE כאסמבלי ופותח **כל דיאלוג** בנפרד לצילום — ביקורת עיצוב מהירה |
| `test-burn.ps1` | בדיקת קצה-לקצה: פותח סרט, F5, לוחץ אישור, ומוודא שנוצר קובץ |
| `test-play.ps1` | בדיקת ניגון |
| `drag.ps1 -X1 -Y1 -X2 -Y2 [-Type "טקסט"]` | גרירת עכבר אמיתית בקואורדינטות הצילום; מדביק טקסט מהלוח |
| `resize.ps1 -W -H` | משנה את גודל החלון ומצלם — לבדיקת מסכים קטנים |
| `test-logic.ps1` | **59 בדיקות לוגיקה בלי ממשק** — זמנים, מסמך, פורמטים, גודל יעד, השוואת גרסאות. טוען את ה-EXE כאסמבלי. להריץ אחרי כל שינוי במנוע |

### מלכודות בכלי הבדיקה — חשוב

1. **כל `.ps1` עם עברית חייב UTF-8 עם BOM.** PowerShell 5.1 קורא קובץ בלי BOM כ-CP1255
   והסקריפט נשבר עם שגיאות פרסור מוזרות. (ראו `[[powershell-hebrew-bom]]` בזיכרון.)
2. **חובה `SetProcessDPIAware()` בסקריפט הצילום.** בלי זה PowerShell מקבל קואורדינטות
   מוקטנות (המסך כאן ב-125%) והצילום חותך את החלון — נראה כמו "באג פריסה" שאינו קיים.
3. **`$args` הוא משתנה אוטומטי ב-PowerShell** — אסור להשתמש בו כשם פרמטר לפונקציה.
   וכן: `@(...)` **משטח אוספים** לתוך המערך, ו-`return $list` פורש רשימה לאובייקטים בודדים —
   בקריאות reflection צריך `Pack` (בונה `object[]` ידנית) ו-`return ,$x`. שתי המלכודות
   האלה מייצרות שגיאת `Parameter count mismatch` / `cannot convert Object[]` מבלבלת.
4. `SendKeys` לא מעביר צירופי Ctrl לאפליקציה הזאת; להשתמש ב-`poke3.ps1`.
   ו-**`SendKeys` עם עברית שולח רצפי Alt+numpad** שפותחים את תפריט המערכת
   וממזערים את החלון. להדביק מהלוח (`Clipboard.SetText` + `^v`).
5. **משתנים ב-PowerShell לא רגישים לרישיות**: `$w` בלולאה דורס את `$W`
   של הפרמטר. זה עלה לי שעה של רדיפה אחרי "באג פריסה" שלא היה.
6. סדר המנייה של `EnumWindows` משתנה בין קריאות; לבחור חלון לפי מחלקה וגודל.
7. הקלקה על חלון שאינו בחזית נבלעת בהפעלה — למקד קודם, ואז להקליק.
8. בדיקות שמחפשות קובץ פלט עם שם עברי — לוודא BOM בסקריפט, אחרת ההשוואה נכשלת בשקט.

---

## 4. מבנה הקוד

| קובץ | אחריות |
|---|---|
| `Program.cs` | `Main`, מקדם DPI (`Theme.Scale`), `Settings` (‏ini ב-`%APPDATA%`), `AppIcon` |
| `Runtime.cs` | פריסת ffmpeg מהמשאב אל `%LOCALAPPDATA%\SubtitleStudio\runtime`, `SplashForm` |
| `Updater.cs` | `App.Version`/`App.Repo`, בדיקת עדכון מ-GitHub, החלפת EXE, `AboutDlg`, `Updates` |
| `MainForm.cs` | החלון הראשי: סרגל, כרטיסים, פריסה, קיצורים, כל פעולות המשתמש |
| `Hero.cs` | מסך הפתיחה (אזור גרירה, שלושה שלבים, קבצים אחרונים, קישורי עזרה למעלה) |
| `Theme.cs` | צבעים, גופנים, `S()` לסקאלה, `Str()` לציור טקסט, `Ltr()` לטקסט טכני |
| `Icons.cs` | כל האייקונים מצוירים בקוד (‏`Ico` enum) — אין גופן אייקונים ואין קבצים |
| `UiKit.cs` | `Btn`, `Card`, `Toggle`, `Slider`, `Field`, `Combo`, `Lbl`, `PopupMenu`, `ScrollHost`, `Ui.Msg` |
| `Timeline.cs` | ציר הזמן: פס קול, בלוקים, גרירה, סימון טווח, זום |
| `CueList.cs` | רשימת הכתוביות (מצוירת ידנית, גלילה מותאמת) |
| `VideoPreview.cs` | תצוגת הפריים + ציור הכתובית מעליו |
| `Model.cs` | `Cue`, `Doc` (‏ביטול/חזרה, מיון, חיתוך טווח), `Tc` (זמנים) |
| `Formats.cs` | קריאה/כתיבה SRT/VTT/ASS/SUB, זיהוי קידוד, יבוא טקסט חופשי, תרגום |
| `Style.cs` | `SubStyle` — עיצוב הכתובית, ציור תצוגה מקדימה, ייצוא שורת ASS |
| `Player.cs` | `Waveform`, `AudioOut` (waveOut), `VideoPipe`, `FrameGrabber`, `Engine` |
| `Ff.cs` | איתור/הרצת ffmpeg, `ProbeFile`, עבודות עם התקדמות ורצף שלבים |
| `Dlg.cs` | בסיס לכל הדיאלוגים + `ProgressDlg` |
| `DlgExport.cs` | הטמעה בסרט (צריבה/ערוץ) + חיתוך קטע |
| `DlgTools.cs` | `MediaTool`/`MediaTools` (17 כלים), `ToolsDlg`, `ToolRunDlg`, `FitPlan` |
| `DlgEdit.cs` | הזזת תזמון, סנכרון, יבוא טקסט, שליפת כתוביות, עיצוב, תיקון אוטומטי |
| `Native.cs` | Win32: DWM (כותרת כהה/פינות), waveOut, EM_SETCUEBANNER |
| `Fonts.cs` | טעינת גופן Assistant המוטמע — **גם** PrivateFontCollection (GDI+) **וגם** AddFontMemResourceEx (GDI) |
| `Ai.cs` | חיבור ל-Gemini: JSON, קריאות פונקציה, תרגום, והצפנת המפתח (DPAPI) |
| `DlgAi.cs` | הסבר והזנת מפתח, חלון תרגום, וחלון התקדמות |
| `DlgChat.cs` | חלון הצ'אט (ללא מסגרת) + `IAiHost` |
| `AiHost.cs` | הצד של התוכנה: אילו פעולות ה-AI רשאי להפעיל ואיך |

---

## 5. מוסכמות ממשק — חובה לשמור עליהן

### סקאלה
כל מידה קבועה עוברת דרך **`Theme.S(v)`**. המסך כאן ב-125% והגופנים גדלים לבד;
מספר קשיח בפיקסלים = טקסט חתוך. אין יוצא מן הכלל.

### טקסט
- ציור טקסט **תמיד** דרך `Theme.Str()`. היא מרחיבה מלבן נמוך מגובה הגופן
  (‏GDI+ פשוט לא מצייר אחרת), ומשתמשת ב-`TextRenderer` לטקסט אטום — חד יותר ומטפל
  נכון ברווחים בעברית (‏`GenericTypographic` בולע רווח באמצע מחרוזת RTL).
- **`Theme.Ltr(s)`** לכל מחרוזת טכנית בתוך טקסט עברי: נתיבים, רזולוציה, `+0.5`, `185 MB`.
  בלי זה הסדר מתהפך (`0.5+`, `MB 185`).

### פקדים
- **אסור `BackColor = Color.Transparent`** על פקד שההורה שלו מצייר בעצמו — WinForms
  מריץ מחדש את ה-Paint של ההורה בתוך הבן, והכותרת משוכפלת לתוך כל פקד.
- מלבן מעוגל ברוחב/גובה אפס מפיל את GDI+. `Theme.RoundRect/FillRound/DrawRound`
  כבר מגנים על זה — לא לעקוף אותם.
- קיצורי מקלדת רק ב-**`ProcessCmdKey`** של `MainForm`. `KeyDown` לא נורה כשהמיקוד
  על פקד ש-`IsInputKey` שלו מחזיר false.

### דיאלוגים
- יורשים מ-`Dlg`; שורות נוספות עם `Row(control, height, gap)` (המידות **לוגיות**, `Row` מכפיל בסקאלה).
- `Buttons(ok, icon, cancel)` סוגר את החלון ומחשב גובה. `BottomButton()` להוספת כפתור נוסף.
- הסתרה/הצגה של שורות → לקרוא ל-**`Restack()`** אחרי השינוי, אחרת נשארים חורים.
  `Restack` קורא את דגל הנראות הפנימי ב-reflection, כי `Visible` מחזיר false כל עוד החלון לא הוצג.

### עיצוב
- מסך הפתיחה: **בלי סרגל כלים ובלי שורת מצב** — רק אזור גרירה, שלושה שלבים, קבצים אחרונים,
  ושלושה קישורים בפינה השמאלית העליונה.
- מסך העבודה: 4 כפתורים בסרגל — **פתיחה · שמירה · כתוביות ▾ · הסרט ▾** — ומשמאל
  הכפתור הראשי (יצירת סרט עם כתוביות), ביטול/חזרה ושלושה מתגי אייקון (עזרה, כהה/בהיר, על התוכנה).
- **הפרדת תחומים היא כלל, לא העדפה:** כל מה שנוגע לכתוביות בתפריט ״כתוביות״ (עם כותרות פנימיות:
  הבאת כתוביות · תזמון · תרגום · מראה), וכל מה שנוגע לקובץ הווידאו בתפריט ״הסרט״.
  ערבוב של השניים הוא באג. `MenuItem.Group("...")` יוצר כותרת קטע בתפריט.
- **תוויות קצרות.** הסרגל מתקפל לאייקונים בלבד כשאין מקום (`compact` ב-`DoLayout`), וזה נראה
  כמו ממשק לא מובן. במסך 1493px ב-125% זה קרה עם תוויות ארוכות — לכן ״פתיחה״ ולא ״פתיחת קובץ״.
  אחרי כל הוספת כפתור לסרגל — לצלם ולוודא שהתוויות עדיין מוצגות.
- כפתור שפותח תפריט מקבל `Menu = true` (חץ קטן) כדי שייקרא כתפריט.
- הכלל הכללי: מינימום פקדים גלויים, מקסימום יכולות מאחורי שני תפריטים ברורים.

---

## 6. מנוע המדיה

- **רק `ffmpeg.exe`. אין `ffprobe`** (חוסך 185MB). כל המידע מגיע מניתוח הפלט של
  `ffmpeg -i` ב-`Ff.ParseFfmpegInfo`, ואיתור פריים מפתח דרך
  `select=eq(pict_type\,I),showinfo`.
- הבילד המצורף הוא ה-full build של gyan.dev, **כולל `libass+fribidi+harfbuzz`** —
  זה מה שמאפשר צריבת עברית RTL נכונה. בילד בלי fribidi ישבור את העברית.
- צריבה: כותבים ASS זמני בשם קצר באנגלית ל-`%TEMP%\SubStudio` ומריצים עם
  `WorkingDirectory` שם, עם `-vf subtitles=<name>.ass`. **לא** להעביר נתיב מלא לפילטר —
  נקודתיים ובקסלשים שוברים אותו.
- עבודות מרובות שלבים (קידוד דו-מעברי): `FfJob.Steps` + `StepNames`.

---

## 7. פריסה ועדכונים

- ה-EXE נושא את ffmpeg דחוס. בהפעלה ראשונה `Runtime.Prepare()` פורס אותו
  (‏~2.5 שניות, עם חלון התקדמות) אל `%LOCALAPPDATA%\SubtitleStudio\runtime\ffmpeg.exe`.
- סדר חיפוש: לצד ה-EXE → `tools\` → `runtime\` → תיקיית המשתמש → `PATH`.
- `portable.txt` לצד ה-EXE = פריסה ל-`runtime\` שלידו (דיסק-און-קי).
- אם בכונן המערכת פחות מ-800MB פנויים — נפרס לצד ה-EXE.
- עדכונים: `Updates.CheckSilent` בכל הפעלה (ניתן לכיבוי), API של GitHub Releases,
  הורדה + סקריפט cmd שמחליף את הקובץ ומפעיל מחדש.

---

## 8. מצב נוכחי

### עובד ונבדק
- יצירה/עריכה/תזמון של כתוביות, ציר זמן עם פס קול, ניגון מסונכרן (‏waveOut + זרם פריימים)
- יבוא SRT/VTT/ASS/SUB/טקסט, זיהוי קידוד כולל Windows-1255
- צריבה בתמונה (עברית RTL נכונה — נבדק על פריים אמיתי), ערוץ כתוביות נפרד, חילוץ כתוביות
- חיתוך (מהיר/מדויק) עם אזהרת פריים מפתח אמיתית, 17 כלי מדיה, קידוד דו-מעברי לגודל יעד
- פריסה מ-EXE יחיד, מצב נייד, ערכה כהה ובהירה, כל הדיאלוגים נבדקו ויזואלית

### שינויים ב-0.4.1 — ביקורת קוד, החזרת כפתורים, ומקש הרווח

**מה שביקורת קוד מצאה, ותוקן:**

1. **חוסם — מסלול העדכון העצמי נשבר על כל נתיב עם עברית.** הסקריפט שמחליף
   את ה-EXE נכתב ב-`Encoding.Default` (‏CP1255) ו-`cmd.exe` קורא **OEM**
   (‏CP862). כלומר `C:\Users\בנימין\...` יצא ג'יבריש. בנוסף לולאת ה-retry
   הייתה **אינסופית**: ההחלפה נכשלת, `cmd` נשאר תקוע לנצח, והתוכנה פשוט
   לא חוזרת. המשתמש רואה תוכנה שנעלמה.
   התיקון: `ShortPathHelper` ממיר לנתיב 8.3 (‏`GetShortPathName`), ואם היעד
   עוד לא קיים — עולה למעלה עד האב הקיים הראשון ומצרף בחזרה את הזנב הלטיני.
   הסקריפט נכתב ב-`Encoding.ASCII`, יש תקרה של 15 ניסיונות, ובכישלון
   מפעילים בחזרה את הקובץ הקיים.
   **`Updater.UpdateScript` הופרד כמתודה ציבורית כדי שאפשר יהיה לבדוק אותה**,
   ו-`test-logic.ps1` מוודא שהפלט ASCII נקי גם לנתיב עברי.

2. **חשוב — סגירת חלון ההורדה התקינה קובץ חתוך.** `DownloadDlg` נסגר
   ב-Alt+F4, הקוד בדק רק `error != null`, וההורדה החלקית נכתבה על ה-EXE.
   עכשיו בודקים גם את `done`.

3. **חשוב — סרט בלי פס קול תקע את הציר ב-30 פריימים לשנייה.**
   `Waveform.Ready` נשאר `false` לנצח כי `Build` לא נקרא, אז הציר צייר
   ״מכין את פס הקול… 0%״ והתרענן בלי סוף. הקלטת מסך היא בדיוק המקרה.
   התיקון: שורה אחת (`else _wave.Ready = true;`), ובדיקה שמריצה `OpenMedia`
   על קובץ בלי אודיו.

4. ״בדיקת עדכון עכשיו״ רצה עכשיו ברקע — קודם היא חסמה את חוט הממשק עד
   12 שניות מאחורי חומת אש, וזה נראה כמו תקיעה.

5. השמטת כתוביות ריקות בשמירה מקבלת `Doc.Push`, אחרת `Ctrl+Z` אחריה קפץ
   לתמונת מצב ישנה.

6. **הבדיקות דרסו את ההעדפות של המשתמש.** `test-layout.ps1` מחליף ערכה
   פעמיים, ו-`ToggleTheme` קורא ל-`Settings.Save` — כלומר כל הרצת בדיקות
   כתבה `dark=0` ל-`settings.ini` האמיתי, והתוכנה נפתחה בהיר.
   עכשיו `Settings.Save` יוצא מיד כש-`SUBSTUDIO_TEST=1`.

**החזרת כפתורים** — המשתמש: ״קיצצת יותר מדי״. חזרו: כוונון ±0.1 לשני
הזמנים, מעבר לכתובית הקודמת/הבאה, תפריט ״עוד״ (חלוקה/איחוד), וסימון
תחילת/סוף קטע וזום על הציר. **אבל עם תקציב רוחב**, אחרת הם דורסים זה את זה:
- שורת הזמנים נושרת בסדר: כפתורי הכוונון → התוויות ״מ־/עד״ → הצרת השדות.
- שורת הפעולות מורידה תוויות **אחת-אחת מהסוף להתחלה**, כך ש״מחיקה״ נשאר
  מכותר הכי הרבה זמן. חץ ופח מובנים בלי מילה; תווית שנעלמת מכולם בבת אחת
  הופכת את השורה לחידה.
- **כפתורי הציר קיבלו שורה משלהם.** קודם כולם הוצבו באותה נקודה בדיוק
  (`b.SetBounds(S(12), wy + S(4), …)` בלולאה) — זה לא התגלה כי היה רק אחד.
  בדיקת הפריסה תפסה את זה מיד ברגע שנוספו עוד.
- `_rangeMarked` — דגל נפרד, כי הפריסה עצמה משנה את `Visible` של הכפתורים.

**מקש הרווח** — המשתמש: ״רווח לא עוצר את הסרטון״. הסיבה: אחרי יצירת
כתובית המיקוד עובר לתיבת הטקסט, ושם רווח הוא רווח. עכשיו:
- רווח **בתיבה ריקה** מנגן/עוצר (עוד לא התחילו להקליד — הכוונה ברורה).
- `Esc` בתיבה משחרר את המיקוד, והרווח חוזר לעבוד.
- `Ctrl`+רווח עובד תמיד. הרמז בשורת המצב תוקן — הוא הבטיח דבר שלא קורה.

**״כאן נגמר״ במצב התזמון** — כפתור שני ליד הכפתור הגדול (`Backspace`),
סוגר את השורה הנוכחית בלי לפתוח את הבאה. זה מה שצריך כשיש שקט בין משפטים.

**`build\make-testmedia.ps1`** מייצר את קובצי הבדיקה (‏`test.mp4` עם קול,
`silent.mp4` בלי), כדי שהבדיקות לא יסתמכו על קבצים שנוצרו ידנית פעם.

### שינויים ב-0.4 — תזמון בלחיצה

הבקשה: ״רוב האנשים לא יודעים לכתוב בפורמט SRT. שיוכלו להקליד משמיעה
בטקסט חופשי, ואז לייבא ולהתאים ויזואלית לציר הזמן.״

**המודל**: `Cue.Untimed` — הזמנים כאן הערכה בלבד. נקבע ב-`ImportPlainText`
כש-`TextImportOptions.MarkUntimed` דלוק **ולשורה אין חותמת זמן משלה**.
המסמך אף פעם לא נשאר במצב שבור: גם בלי לתזמן, יש זמנים סבירים לפי קצב קריאה.

**המצב** (`MainForm`, `_tapping` + `_tapIndex`):
- `_tapBar` — פס שמופיע רק כשיש שורות לא מתוזמנות **ויש סרט פתוח**.
- הכפתור הגדול מחליף תפקיד בתוך המצב: ״כאן מתחיל: «הטקסט»״.
- `TapHere()` בלחיצה אחת: סוגר את הקודמת (‏`pos-40`, **חסום** ל-`GuessDur+1500`
  כדי שכתובית לא תישאר תלויה בשקט ארוך), פותח את הנוכחית, ומוריד את הדגל.
- **לא קוראים ל-`Doc.Sort()` באמצע המצב** — הבאות עדיין נושאות זמני הערכה
  שעלולים להיות מוקדמים יותר, ומיון היה מערבב את הסדר ושובר את `_tapIndex`.
  במקום זה דוחפים את הלא-מתוזמנות אחרי הנוכחית בלולאה.
- `Enter` = לחיצה, `Esc` = יציאה. שניהם נתפסים **לפני** בדיקת `InTextBox`,
  כי במצב הזה המקלדת שייכת למצב.
- כל לחיצה היא `Doc.Push` נפרד, אז `Ctrl+Z` מבטל לחיצה אחת.

**`ImportTextDlg` נכתב מחדש** — היו שמונה פקדים גלויים, נשארו שלושה:
תיבה, שורת סיכום, ומתג ״לתזמן לפי הסרט״. החלוקה מזוהה אוטומטית
(`LooksLikeParagraphs`), והשאר מאחורי ״הגדרות מתקדמות״ עם `Restack()`.
הבנאי הישן `ImportTextDlg(long)` נשמר כי סקריפטי הבדיקה בונים אותו.

**ברשימה** זמן לא מתוזמן מצויר עם ״≈״ וב-`TextFaint`.

**ה-AI** קיבל `tap_later` ב-`create_subtitles_from_text`.

**14 בדיקות חדשות ב-`test-ui.ps1`** מריצות את כל הזרימה בלי מדיה אמיתית:
מזייפות `_mi` ב-reflection, מזיזות את הנגן ב-`Engine.Seek` (עובד גם בלי קובץ),
וקוראות ל-`TapHere` — כולל האימות שהכתובית הראשונה **לא** נמתחת עד הלחיצה הבאה.

### שינויים ב-0.3.1 — מסך הפתיחה, לפי השראה שהמשתמש נתן

המשתמש הפנה ל-Video2Audio (פוסט במתמחים) כרפרנס. מה שאומץ:
כותרת גדולה שבה **מילה אחת בצבע המותג**, אזור גרירה רחב עם מסגרת מקווקוות
ברורה, **שני** כפתורים בתוכו, וצ׳יפים ממוסגרים בפינה במקום אייקונים חשופים.

- `HeroPanel.DrawTitle` מצייר את הכותרת בשני קטעים: ״אולפן״ ב-`Theme.Text`
  ו״הכתוביות״ ב-`Theme.Accent`.
- אזור הגרירה: `Math.Min(S(720), Width*0.72)` (היה `S(560)`), גובה `S(238)`,
  והכפתורים ממוקמים **יחסית לתוכן** (`z.Y + S(156)`) ולא לתחתית — אחרת נפער
  חלל באמצע כשהאזור גבוה.
- `_openSubs` — כפתור שני, ״קובץ כתוביות״, מסנן ל-SRT/VTT/ASS/SUB/TXT
  (`MainForm.OpenSubsDialog`). דרך כניסה אמיתית: לתקן תזמון של קובץ קיים.
- `DrawLinks` מצייר צ׳יפ ממוסגר תמיד, לא רק בריחוף.

**שתי מלכודות RTL שנפלנו בהן כאן:**
1. מלבן שרוחבו **בדיוק** הרוחב הנמדד גורם ל-`Trimming` של `SfCenter` לחתוך
   את סוף המחרוזת — הכותרת יצאה ״או... ...הכתוב״. צריך שוליים במלבן.
2. **רווח בסוף מחרוזת RTL נבלע** (אותה מלכודת שמתועדת ב-`Theme.Str`).
   ״אולפן ״ + ״הכתוביות״ הודבקו. הפתרון: למדוד בלי הרווח ולהוסיף מרווח
   בפיקסלים בין הקטעים.

**מצב כהה כברירת מחדל** — `Theme.Dark = true` כבר היה ברירת המחדל בקוד;
מי שראה בהיר, ראה את זה כי `settings.ini` שלו כלל `dark=0` מגרסה קודמת.
אין שינוי קוד, רק אימות. התוכנה לא עוקבת אחרי ערכת המערכת בכוונה.

**צילומי המסך ב-`docs/screenshots` רועננו.** הישנים הראו את הממשק שלפני 0.2
ואת כפתורי הקישור עם תוויות טקסט. הוסרו `sync.png` ו-`start-light.png` הישנים.
`start.png` הוא עכשיו כהה, `start-light.png` בהיר במסך מזערי.

**בדיקת מסך הפתיחה** נוספה ל-`test-layout.ps1`: `HeroPanel` אין לו פקדי בן,
אז במקום חפיפות מפעילים את חישובי הגאומטריה הפרטיים ב-reflection
(`Zone`, `StepsTop`, `BlockTop`, `RecentRow`) ומכריחים `DrawToBitmap` —
שגם תופס קריסות GDI+ בציור.

**`test-layout.ps1` כבר לא איטי** — 3.1 שניות. מה שהאט אותו היה
`Updates.CheckSilent` שרץ ארבע פעמים מול הרשת; `SUBSTUDIO_TEST=1` פתר את זה.

### שינויים ב-0.3 — העדפות, רישיונות, ובדיקות שמחליפות עכבר

**עוצמה ומהירות נשמרות** (`Settings.Volume` / `Settings.Speed` ב-`Program.cs`).
נכתבות ב-`FormClosing` דרך `SaveAll()`, והמהירות מוחלת ב-`Shown`.

**רישוי — הפער שסגרנו**
`THIRD-PARTY.md` דיבר רק על FFmpeg, ואמר במפורש ״אין גופנים״ — אבל ה-EXE מטמיע
שלושה קבצי Assistant תחת OFL 1.1, שדורש שהרישיון ילווה את ההפצה. עכשיו:
- `licenses/GPL-3.0.txt` ו-`licenses/OFL-1.1-Assistant.txt` (הנוסחים המקוריים),
  ועותק OFL גם ב-`assets/fonts/` ליד הגופנים עצמם.
- **שלושת הנוסחים מוטמעים ב-EXE** (`/resource:` ב-`build.cmd`), ו״על התוכנה״
  מקבל מקטע ״רישיון וקוד מקור״ עם כפתור ששומר אותם לתיקייה (`SaveLicenses`).
  ככה הקובץ הבודד נושא את התיעוד המשפטי של עצמו, בלי תלות באינטרנט.
- `THIRD-PARTY.md` נכתב מחדש: טבלת תקציב זכויות, מקטע ״רוצים לקחת את הקוד״
  שמעודד פורק ל-macOS, הצעת קוד מקור לפי GPLv3 §6, ומקטע גופן.
- **תוקן אי-דיוק**: המסמך הזהיר ש-`essentials` אולי בלי `libfribidi`.
  בדקנו — `9.0.1-essentials_build` כולל `libass+libfribidi+libharfbuzz`.

**לוגו** — `build\make-logo.ps1` מצייר את אותו סמל של האייקון וקטורית ומרנדר
ל-PNG עד 1024, כולל גרסאות עם שם ולרקע כהה. הפלט ב-`docs\logo`.
מלכודת PowerShell: `New-Object Font $fam, [single]$x, $st` שולח את **המחרוזת**
`"[single]132"` לבנאי; חייבים `([single]$x)` בסוגריים או `-ArgumentList`.

**`build\test-ui.ps1` חדש — 17 בדיקות ממשק בלי עכבר.**
בונה את החלון בתוך התהליך, מחוץ למסך, ומפעיל `OnClick` ישירות ב-reflection.
בודק: הכפתור הכחול יוצר כתובית במיקום הנגן ומסמן אותה, `_volBtn` פותח
`PopupPanel` ו-`_speedBtn` פותח `PopupMenu`, הסליידר חוזר לכרטיס אחרי סגירה
ואפשר לפתוח שוב, `Ctrl`+חצים קופצים 2000/500ms, `Ctrl`+חצים אנכיים משנים
מהירות, כתובית ריקה מושמטת בשמירה, ותוויות התפריטים בסרגל שורדות ב-1175.

**למה לא לחיצות אמיתיות** — `SetForegroundWindow` נחסם כשהתהליך הקורא אינו
בחזית. אז הקליק נוחת על חלון אחר, `mouse_event` לא מחזיר שגיאה, והבדיקה
״עוברת״ בשקר. בזבזתי על זה זמן; אל תחזרו לשם.

**`test-layout.ps1` כולל עכשיו גם 13 דיאלוגים** (בנייה, חפיפות, גלישה,
וגובה מול המסך). `ScrollHost` פטור מבדיקת גלישה — ילדים מתחת לאזור הנראה
הם בדיוק המשמעות של גלילה.

**`test-logic.ps1` בודק שהרישיונות מוטמעים** — אם מישהו ימחק שורת `/resource`
מ-`build.cmd`, ההפצה תיהפך ללא-תואמת בשקט. הבדיקה תופסת את זה.

### שינויים ב-0.2.1 — ליטוש אחרי החלפת ה-UX

**הסרגל מוותר על תוויות אחת-אחת** (`DoLayout`, מערך `dropOrder`).
קודם `compact` הוריד את כולן בבת אחת ונשארו ארבעה אייקונים בלי הסבר.
הסדר: שמירה → פתיחה → הסרט → כתוביות, כלומר לפי כמה קשה לנחש את הכפתור
בלי תווית. שני התפריטים נשארים מכותרים כמעט תמיד.

**סליידר העוצמה עבר ל-`PopupPanel`** (חדש ב-`UiKit.cs`) — חלונית קופצת
כללית שמארחת פקד אחד, עם אותו מראה של `PopupMenu`. `_volume` חי קבוע
ב-`_videoCard` כשהוא מוסתר (כדי שהחלפת ערכה תגיע גם אליו) ומועבר לחלונית
בכל פתיחה. שורת הניגון התפנתה, והשעון מקבל את כל מה שנשאר בין הקבוצות.

**כתובית בלי טקסט מושמטת בשמירה** (`SaveSubtitles`), כדי שהמספור בקובץ
יתאים לרשימה. אם כולן ריקות — הודעה, בלי לכתוב קובץ.

**קפיצה אחידה של שתי שניות** בכפתורי הניגון ובחצים (היה 5 בכפתורים
ו-2 בחצים אחרי סבב 0.2 — חוסר התאמה).

**מסך העזרה** (`DlgHelp.cs`) עודכן לזרימה החדשה ול-18 קיצורים.

**`SUBSTUDIO_TEST=1`** — משתנה סביבה שמדלג ב-`Shown` על בדיקת עדכון,
שחזור אוטומטי ודיאלוג ״לא נמצא FFmpeg״. בלעדיו בדיקת הפריסה נתקעה על
דיאלוג מודאלי ופנתה לרשת ארבע פעמים. `test-layout.ps1` מדליק אותו.

**`test-layout.ps1` גדל**: בדיקת חפיפות וגלישה בארבעה גדלים, בדיקה
ש-`PopupPanel` נפתח ומארח פקד, ו**בדיקת מצב כהה** — אחרי `ToggleTheme`
אף פקד לא נשאר עם רקע בהיר (בדיוק הבאג שהיה).

### שינויים בסבב 0.2 — החלפת ה-UX

המשתמש אמר: ״ניסיתי להשתמש ולא הבנתי איך משתמשים… פחות אזורים, פחות כפתורים
שאף אחד לא יודע מה עושים״. לכן הוחלף המבנה, לא רק העיצוב.

**שני אזורים במקום ארבעה**
`_tlCard = _videoCard` ו-`_editCard = _listCard` — הציר נכנס לכרטיס הסרט,
והעורך נכנס לכרטיס הרשימה (מופרד בקו דרך `Card.SepY`). מימין ״הכתוביות״,
משמאל ״הסרט״, וביניהם אין כלום.

**כפתור אחד גדול**
`_addCueBtn` — ״כתובית חדשה כאן״ ברוחב מלא מתחת לפס הקול. `NewCueAtPlayhead`
**עוצר קודם את הניגון** ואז יוצר ומעביר מיקוד לתיבת הטקסט.

**מה ירד מהמסך**
כתובית חדשה/הקודמת/הבאה/עוד, ארבעה כפתורי כוונון, זום, ותחילת/סוף קטע.
חלוקה ואיחוד עברו ל**קליק ימני על הרשימה** (`CueList.ContextRequested` →
`ShowCueMenuAt`), סימון קטע ל-`I`/`O`, וזום ל-Ctrl+גלגלת.

**מצב ריק מלמד**
`CueList.DrawSteps` מצייר שלושה שלבים ממוספרים במקום משפט עמום, ופס כותרות
הטבלה לא מצויר בכלל כשאין שורות. הרמז על פס הכתוביות בציר הונמך למשפט אחד
שקט, כדי שלא יתחרה בכפתור הכחול.

**מהירות השמעה** (`Engine.Speed`, ‏0.5–2)
`AudioOut` מוסיף `-af atempo=…` (‏`TempoChain` משרשר עותקים מחוץ לטווח 0.5‑2)
ו-`PositionMs` מכפיל את מה שהמונה סופר ב-`_playSpeed`, כי waveOut סופר את
**הפלט**. ‏`VideoPipe` לא השתנה: הפריימים חתומים בזמן המקור ו-`Take(Position)`
בוחר לבד. שינוי מהירות תוך ניגון עושה Pause/Play מאותו מקום.
כפתור `_speedBtn` עם `Ico.Gauge` ליד סליידר העוצמה.

**קיצורים שעובדים גם בתוך תיבת הטקסט** — בדיוק אז רוצים לחזור ולשמוע שוב:
`Ctrl`+חצים = 2 שניות (עם Shift חצי שנייה), `Ctrl`+רווח = ניגון/עצירה,
`Ctrl`+חצים למעלה/מטה = מהירות. חצים בלי Ctrl עובדים רק מחוץ לתיבה.

**ה-AI קיבל 17 פעולות נוספות** (סה״כ 32) כי הצ׳אט סירב לדברים שהתוכנה כן
עושה: `create_subtitles_from_text`, `extract_subtitles_from_video`,
`open_file`, `save_subtitles_as`, `set_cue_times`, `split_cue`, `merge_cues`,
`replace_text`, `wrap_lines`, `clear_all_cues`, `stretch_timing`,
`play_pause`, `set_playback_speed`, `undo`, `set_theme`, `list_media_tools`,
ו-`run_media_tool` שמריץ כלי לפי שם דרך `ToolsDlg.RunNamed`.
ההנחיה ב-`DlgChat.SystemPrompt` מונה את היכולות במפורש ומורה לבדוק את רשימת
הפונקציות לפני שאומרים ״אני לא יכול״.

**באגי פריסה שנמצאו אוטומטית — ראו `build\test-layout.ps1`**
שורת העריכה ושורת הניגון חושבו במידות קבועות ונדרסו בחלון צר.
עכשיו שתיהן מחשבות תקציב רוחב: התוויות ״מ־/עד״ יורדות, השדות מתכווצים,
סליידר העוצמה מתקצר, והשעון מציג רק מיקום מתחת ל-`S(140)` (`ClockText`).

### כלי בדיקה חדשים
| סקריפט | מה הוא עושה |
|---|---|
| `test-layout.ps1` | בונה את החלון בארבעה גדלים **מחוץ למסך** + 13 דיאלוגים, ומדווח על פקדים שחופפים או גולשים. כולל בדיקת מצב כהה |
| `test-ui.ps1` | בדיקות ממשק בלי עכבר: מפעיל `OnClick` ב-reflection ובודק שהכפתורים באמת עושים את מה שכתוב עליהם, כולל מצב התזמון וסרט בלי קול |
| `make-testmedia.ps1` | מייצר את קובצי המדיה שהבדיקות צריכות |
| `test-speed.ps1` | מודד שהמנוע באמת מנגן ב-0.5/1/1.5 — משווה זמן-סרט לזמן-קיר |
| `make-logo.ps1` | מייצר את הלוגו ל-`docs\logo` ב-PNG עד 1024 |

### שינויים אחרונים (סבב פריסה, מצב כהה ואייקונים)

**פריסה במסך קטן — היה שבור**
- `MinimumSize` ירד מ-1120x720 ל-**940x680**, והגודל ההתחלתי נחתך למסך (`FitToScreen`).
  קודם התוכנה לא נכנסה במחשב נייד 1366x768.
- תקציב הגובה של הכרטיסים מחולק לפי עדיפות (`videoMin`/`editMin`/`tlMin`)
  במקום מידות קבועות שדחסו את הווידאו ל-40 פיקסל.
- שורת הניגון מתכווצת, כפתורי העריכה הופכים לאייקונים (`PrefWidth`),
  ומסך הפתיחה מוותר על הקבצים האחרונים כשאין גובה.
- **לא מחשבים פריסה כשהחלון ממוזער** — רוחב 200 פיקסל קבע מצב מצומצם קבוע.

**מצב כהה — היה שבור לגמרי**
פקד שומר את הצבע שנקבע לו בבנייה, אז החלפת ערכה השאירה חצי מסך לבן.
`Theme.Palette()` + `Theme.Swap()` ממפות כל צבע מהפלטה הישנה לחדשה בכל עץ
הפקדים (כולל שדות `Color` ציבוריים דרך reflection), ו-`Theme.Reapply()` קובעת רקעים
לפי סוג הפקד. **כל פקד חדש ששומר צבע בשדה ציבורי עובד אוטומטית.**

**אייקונים**
צוירו מחדש: ביטול/חזרה, מספריים, מפתח, בועת שיחה, גלגל שיניים (`Donut`),
פיצול/איחוד, להבה, דיסקט. הקישורים במסך הפתיחה הם כפתורי אייקון בלבד.

**עוד תיקונים**
- פס הקול נמתח לשיא הקובץ (`Waveform.Recalc`) — הקלטה שקטה נראתה קו ישר
- שדה נתיב גולל לסוף (`Field.PathMode`) — שם הקובץ חשוב מאות הכונן
- תיבת ההודעה: הכותרת כיסתה את האייקון
- ״קטע מסומן״ רק כשיש קטע אמיתי, ותיבת הטקסט מוליכה בכתובית ריקה
- כל סקריפטי הבדיקה עברו ל-`%TEMP%\ss-gallery` ולא תלויים במספר הסשן

### שינויים בסבב ה-AI והעיצוב

**גופן ועומק ויזואלי**
- גופן **Assistant** (רישיון OFL) מוטמע ב-EXE בשלושה משקלים (`assets\fonts`, ~223KB).
  `Fonts.cs` טוען אותו **פעמיים**: PrivateFontCollection ל-GDI+ ו-AddFontMemResourceEx ל-GDI.
  בלי השנייה כל טקסט שנובע מ-`TextRenderer` חוזר ל-Segoe UI והמסך נראה מעורבב.
- מידות טיפוגרפיה גדלו, כותרות בחצי-מודגש (`Theme.Semi`), וצל רך לכרטיסים (`Theme.Shadow`)
  במקום מסגרת בעובי פיקסל.

**עריכת כתוביות**
- ציר הזמן: פס הכתוביות קיבל רקע וכותרת. **גרירה על פס הקול = ניווט,
  גרירה על פס הכתוביות = יצירת כתובית** (היה Alt+גרירה נסתר).
- פאנל העריכה: שתי שורות זמן עם כפתורי כוונון וכפתור ״מהסרט״, מעבר הקודמת/הבאה,
  ותצוגת משך וקצב קריאה. גובה הכרטיס 192 (היה 164).

**AI**
- `Ai.cs` — Gemini דרך REST עם `System.Web.Extensions` (JSON) ו-`System.Security` (DPAPI למפתח).
  נוספו שני `/reference:` ל-`build.cmd`. המפתח נשמר מוצפן ב-`settings.ini`.
- תרגום עובד במנות של 40 כתוביות עם `responseMimeType: application/json`.
- צ'אט עם קריאות פונקציה: 15 פעולות ב-`AiHost.cs`. **חובה להחזיר לשרת את תור המודל
  עם `functionCall` לפני ה-`functionResponse`** — אחרת הבקשה נדחית.
- קיצור: `Ctrl+K`. כפתור סגול בסרגל.

**הבאג שהשבית את כל ה-AI**
`JavaScriptSerializer.Deserialize<Dictionary<string,object>>` מחזיר **ArrayList**
למערכים מקוננים, לא `object[]`. כל ההמרות `as object[]` החזירו null, ולכן שום
תשובה מהמודל לא נקראה — התוכנה אמרה ״השרת החזיר תשובה לא צפויה״ גם כשהתשובה
הייתה מושלמת. הפתרון: `DeserializeObject` + עוזר `Arr()` שמקבל כל `IList`.
**נבדק מקצה לקצה עם מפתח אמיתי:** תשובה רגילה, קריאת פונקציה (`get_state`),
ותרגום עברית→אנגלית — הכל עובד.

**מלכודות שנפלנו בהן הפעם**
- `RightToLeftLayout = true` על טופס שמצייר בעצמו משקף את כל הציור — האותיות יוצאות הפוכות.
  מספיק `RightToLeft = Yes`.
- הוספת `case` חדש לפני `case Ico.Sync:` ניתקה את `Ico.Refresh` מהגוף שלו —
  וכפתור הרענון צייר פתאום מטרה. להוסיף אייקונים רק במקום שלא שובר קבוצות `case`.
- תיבת טקסט מרובת שורות לא תומכת ב-EM_SETCUEBANNER; הרמז ממומש ידנית.
- `Btn` מסוג `Tool` התעלם מ-`Tint`; תוקן כדי שכפתור ה-AI יהיה סגול.

### שינויים בסבב הפרסום
- **נכסי גיטהאב**: `README.md` דו-לשוני וקצר (פונקציונליות בלבד — בלי סיפורי רקע),
  `LICENSE` (MIT), `THIRD-PARTY.md` (‏FFmpeg תחת GPLv3 + הצעת קוד מקור), `CHANGELOG.md`,
  `.gitignore`, `.gitattributes`, `tools\README.md`, צילומי מסך ב-`docs\screenshots`
- **מאגר git אותחל** עם קומיט ראשון (הכתובת `237136058+BeniaBot@users.noreply.github.com`)
- **`build\test-logic.ps1` חדש** — 59 בדיקות אוטומטיות, כולן עוברות
- מסך העבודה: ציר הזמן מתאים את עצמו לכל אורך הסרט עד שהמשתמש משנה זום בעצמו
  (‏`_userZoomed`), פס גלילה נעלם כשאין מה לגלול, עמודת הטקסט ברשימה התרחבה
- מסך הפתיחה: שם התיקייה מוצג צמוד לשם הקובץ ולא בקצה השני של השורה
- חלון ״יבוא טקסט״ ירד מ-1005px ל-880px (מתאים למסכים קטנים)
- מצב ריק ברשימת הכתוביות עבר ל-`Theme.S()` (היה בפיקסלים קשיחים)

### שינויים בסבב הקודם
- הופרד תפריט אחד מעורבב לשני תפריטים לפי תחום, עם כותרות קטעים
- קוצרו תוויות הסרגל כדי שלא ייקרס לאייקונים
- נוסף שבב ״קטע מסומן HH:MM–HH:MM״ בכותרת הציר + כפתור ניקוי שמופיע רק כשיש סימון
- נוסף מנגנון עדכונים (בדיקה בכל הפעלה, ניתן לכיבוי) ו-`AboutDlg`
- תוקנו: זום שקרס כשהציר עוד לא נפרס, תווית משך שלא התרעננה, סימני +/- הפוכים ב-RTL,
  פס גלילה לבן בתיבת טקסט, כפתור שגלש מהדיאלוג, קריסת GDI+ על מלבן ברוחב אפס

### לגרסה הבאה

- **OCR לכתוביות תמונה (PGS/VobSub) — נבדק ונדחה.** ‏Windows מביא מנוע OCR
  מובנה (`Windows.Media.Ocr`) שאפשר להגיע אליו גם מ-‎.NET Framework דרך
  `/reference:` ל-winmd. **אבל** `OcrEngine.AvailableRecognizerLanguages`
  במכונה הזאת מחזיר `en-US` בלבד, ועברית איננה ברשימת השפות שמיקרוסופט
  תומכת בהן ב-OCR כלל. כלומר: עבודה לא קטנה, שלא תיתן כלום לקהל היעד.
  אם בכל זאת — Tesseract חיצוני, לא מוטמע.
- **חתימת קוד** — ווינדוס מזהיר בכל הפעלה. תעודה עולה כסף. בינתיים כדאי
  לפרסם SHA-256 של כל מהדורה כדי שאפשר יהיה לוודא שהקובץ לא שונה.
- **`docs/screenshots/burned-frame.png`** לא רוענן; הוא לא תלוי בממשק.

### ידוע ופתוח
- כתוביות תמונה (PGS/VobSub) — חילוץ בלבד, אין OCR

### פורסם
**https://github.com/BeniaBot/subtitle-studio** — מאגר ציבורי, תגית `v0.1.0`,
מהדורה עם `SubtitleStudio.exe` (72.8MB) כנכס להורדה.
מנגנון העדכון נבדק מול המהדורה החיה (`build	est-update.ps1`) ✅

**לגרסה הבאה:** להעלות את `App.Version`, לבנות, `git tag vX.Y.Z`,
`gh release create` עם ה-EXE. המשתמשים יקבלו הצעת עדכון בהפעלה הבאה.

**קומיטים עם `237136058+BeniaBot@users.noreply.github.com`** (הכתובת ב-gmail לא מאומתת)

### רישוי — לשים לב
ה-EXE מכיל בינארי של FFmpeg תחת **GPLv3**. הקוד שלנו MIT, אבל ההפצה של ה-EXE
כפופה ל-GPLv3, ולכן `THIRD-PARTY.md` חייב להתלוות למהדורה (קרדיט, גרסת הבילד,
וקישור לקוד המקור). ffmpeg מורץ כתהליך נפרד — אין קישור קוד.

---

## 9. עבודה במקביל בכמה סשנים

- **לתעד כאן כל שינוי מיד בסיומו** — זה הקובץ שממנו סשן אחר מתחיל.
- לא להריץ שתי בניות במקביל: `dist\SubtitleStudio.exe` ננעל.
- לפני בנייה: `Get-Process SubtitleStudio | Stop-Process -Force`.
- קבצי בדיקה זמניים נכתבים ל-scratchpad של הסשן, לא לתיקיית הפרויקט.

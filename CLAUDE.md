# Subtext (אולפן הכתוביות) — מסמך עבודה

**קראו את הקובץ הזה לפני כל שינוי בפרויקט.** הוא מתעד את הארכיטקטורה, המוסכמות,
המלכודות שכבר נפלנו בהן, ואת מצב העבודה הנוכחי. כל סשן שמשנה משהו — מעדכן כאן מיד.

---

## 1. מה זה

תוכנת דסקטופ לווינדוס ליצירה, תיקון והטמעה של כתוביות בעברית.
**קובץ EXE יחיד, בלי התקנה, בלי אינטרנט, בלי ספריות חיצוניות.**

- קוד: `src\*.cs` (C#, WinForms)
- פלט: `dist\Subtext.exe` (~38MB — הכל בפנים)
- בנייה: `build.cmd`
- גרסה נוכחית: `App.Version` ב-`src\Updater.cs` (כרגע `0.8.0`, שוחררה)

---

## 2. בנייה

```
build.cmd
```

1. יוצר `build\app.ico` אם חסר (`build\make-icon.ps1`, מצייר את האייקון בקוד).
2. אורז את `tools\ffmpeg.exe` ל-`build\payload\ffmpeg.pack` (‏102MB → 36MB, Deflate).
   הפעולה מדלגת אם החבילה עדכנית. למחוק את הקובץ כדי לאלץ אריזה מחדש.
3. מהדר עם **המהדר המובנה בווינדוס**:
   `%WINDIR%\Microsoft.NET\Framework64\v4.0.30319\csc.exe`
   ומטמיע את `ffmpeg.pack` כמשאב.

### אילוצים קריטיים בבנייה

| אילוץ | פירוט |
|---|---|
| **C# 5 בלבד** | המהדר הזה הוא מ-.NET Framework. **אין** אינטרפולציית מחרוזות (`$"..."`), `?.`, `nameof`, מאתחלי מאפיינים, `out var`, פונקציות מקומיות. יש `async`, למדות ו-`delegate`. |
| **‎.NET Framework 4.8** | מובנה בווינדוס 10/11. אין SDK, אין NuGet, אין חבילות. |
| **`/codepage:65001`** | קבצי המקור הם UTF-8 בלי BOM והמהדר חייב את הדגל הזה כדי לקרוא עברית נכון. |
| **`build.cmd` חייב להיות ASCII** | cmd עוקב אחרי היסטי בייטים; תו עברי אחד אפילו בהערה שובר את הריצה. ההודעות בו באנגלית בכוונה. |
| **לסגור את התוכנה לפני בנייה** | אחרת `dist\Subtext.exe` נעול והקומפילציה נכשלת עם `CS0016`. |

---

## 3. כלי הבדיקה (`build\*.ps1`)

| סקריפט | מה הוא עושה |
|---|---|
| `test-all.ps1 [-Only a,b]` | **כל החבילות**, כל אחת מול מספר הבדיקות שהיא חייבת להגיע אליו, עם הגבלת זמן. חבילה שנתקעה - `hang-report` מדפיס למה |
| `test-run.ps1 -Wait N -Shot x.png [-Media path]` | בונה **דרך `build.cmd`**, מפעיל, מצלם (‏PrintWindow). **משאיר את התוכנה פתוחה** - לסגור לפני בנייה (`CS0016`). עם `SUBSTUDIO_TEST` הוא לא פותח את הקובץ שהועבר |
| `shots.ps1 [-Only a,b] [-Light] [-Lang en]` | **כל החלונות לתמונות, מחוץ למסך** - בלי לגנוב מיקוד. ‏`%TEMP%\ss-shots` |
| `parts.ps1 [-Light]` | הרשימה וציר הזמן לבד, עם שורה נבחרת/בריחוף/מתנגנת ובלוק נבחר |
| `icon-sheet.ps1` / `compare-shots.ps1` | כל האייקונים בכל הגדלים; אותו אזור משני צילומים, מוגדל, אחד מעל השני |
| `hang-report.ps1 [-Kill]` | בדיקה ״תקועה״ היא לרוב חלון ״Unhandled exception״ שמחכה ללחיצה. פותח את הפרטים ומדפיס את המחסנית |
| `tr-probe.ps1 -Media f -Starts 55,110` | תמלול של קטע אחד בדיוק כמו התוכנה, עם המפתח של המשתמש, ושומר את התשובה **הגולמית** |
| `poke.ps1 -Keys "f5,esc"` | שולח מקשים בודדים ב-PostMessage (בלי מודיפיירים) ומצלם |
| `poke3.ps1 -Key "T"` | שולח **Ctrl+אות** ב-keybd_event אמיתי (PostMessage לא עובד למודיפיירים) |
| `click.ps1 -X -Y` | לחיצת עכבר אמיתית בקואורדינטות לקוח, ומצלם |
| `dialog-gallery.ps1` | טוען את ה-EXE כאסמבלי ופותח **כל דיאלוג** בנפרד לצילום — ביקורת עיצוב מהירה |
| `test-burn.ps1` | בדיקת קצה-לקצה: פותח סרט, F5, לוחץ אישור, ומוודא שנוצר קובץ |
| `test-play.ps1` | בדיקת ניגון |
| `drag.ps1 -X1 -Y1 -X2 -Y2 [-Type "טקסט"]` | גרירת עכבר אמיתית בקואורדינטות הצילום; מדביק טקסט מהלוח |
| `resize.ps1 -W -H` | משנה את גודל החלון ומצלם — לבדיקת מסכים קטנים |
| `test-logic.ps1` | **177 בדיקות לוגיקה בלי ממשק** — זמנים, מסמך, פורמטים, גודל יעד, השוואת גרסאות. טוען את ה-EXE כאסמבלי. להריץ אחרי כל שינוי במנוע |
| `test-project.ps1` | **105** — קובץ פרויקט, שמירה אוטומטית, שחזור, שיוך סיומת (דורש מתקין בנוי) |
| `test-real-open.ps1` | **הרצה אמיתית** של ה-EXE עם קובץ פרויקט, צילום, וסגירה בלי ״לשמור?״ |
| `test-screens.ps1` | **32**: כל חלון ותפריט מול 1080p ב-150%, ‏1366×768 ו-1080p ב-125%, בקנה מידה אמיתי |
| `test-buttons.ps1` | **10**: כל כפתור בכל חלון: תוכן ממורכז, לא נחתך, לא נוגע בקצה (181 כפתורים). **נעצרת אם הגופן המוטמע לא נטען** |
| `test-errors.ps1` | **36**: הודעות שגיאה אנושיות מול כשלים אמיתיים, וחלון ההתקדמות **באמצע עבודה** |
| `test-stt.ps1` | **59**: שירות התמלול מול שרת מדומה; פירוש תשובת גוגל בכל צורת זמן; שורות בלי זמן; לולאות חזרה |
| `test-settings.ps1` | **9**: שמירת ההגדרות על קובץ זמני: מפתח נשמר, חלון אחר לא מוחק אותו, כישלון לא נבלע |
| `test-source.ps1` | **4**, בלי בנייה: תווי בקרה, BOM, ‏`Ltr` סביב עברית, גודל קובץ בלי `Ltr` |
| `test-release.ps1` | **42**: קריאת תשובת גיטהאב, ובדיקת ההורדה (טביעה, קובץ קטוע, דף חסימה) מול שרת מדומה |
| `test-qa.ps1` | **60**: בדיקת השגיאות: הכללים בגבולות, התיקון, 300 מסמכים אקראיים, והתג בממשק |
| `test-spell.ps1` | **59**: בדיקת האיות: המנוע מול המילון האמיתי, ההורדה מול שרת מדומה, והממשק |
| `test-long.ps1` | **15**: שיעור של 3 שעות. פס קול, תזמון 2,900 שורות, פרויקט של 5,000, ציור |
| `test-fuzz.ps1` | **11**: 1,600 קובצי כתוביות משובשים, בלי חריגה ובלי היתקעות |
| `test-upgrade-real.ps1 -From vX` | **שדרוג אמיתי** מתגית קודמת לגרסה שב-`dist`, דרך מנגנון העדכון. להריץ אחרי שחרור |
| `test-scenecuts.ps1` / `test-autotime.ps1` | 47 / 21 — מעברי סצנה ותזמון לפי הדיבור, מול זמנים ידועים |

### מלכודות בכלי הבדיקה — חשוב

1. **כל `.ps1` עם עברית חייב UTF-8 עם BOM.** PowerShell 5.1 קורא קובץ בלי BOM כ-CP1255
   והסקריפט נשבר עם שגיאות פרסור מוזרות. (ראו `[[powershell-hebrew-bom]]` בזיכרון.)
2. **חובה `SetProcessDPIAware()` בסקריפט הצילום.** בלי זה PowerShell מקבל קואורדינטות
   מוקטנות (המסך כאן ב-125%) והצילום חותך את החלון — נראה כמו "באג פריסה" שאינו קיים.
3. **`$args` הוא משתנה אוטומטי ב-PowerShell** — אסור להשתמש בו כשם פרמטר לפונקציה.
   וכן: `@(...)` **משטח אוספים** לתוך המערך, ו-`return $list` פורש רשימה לאובייקטים בודדים —
   בקריאות reflection צריך `Pack` (בונה `object[]` ידנית) ו-`return ,$x`. שתי המלכודות
   האלה מייצרות שגיאת `Parameter count mismatch` / `cannot convert Object[]` מבלבלת.
4. `SendKeys` לא מעביר צירופי Ctrl לאפליקציה הזאת; להשתמש ב-`poke3.ps1`.
   ו-**`SendKeys` עם עברית שולח רצפי Alt+numpad** שפותחים את תפריט המערכת
   וממזערים את החלון. להדביק מהלוח (`Clipboard.SetText` + `^v`).
5. **משתנים ב-PowerShell לא רגישים לרישיות**: `$w` בלולאה דורס את `$W`
   של הפרמטר. זה עלה לי שעה של רדיפה אחרי "באג פריסה" שלא היה.
6. סדר המנייה של `EnumWindows` משתנה בין קריאות; לבחור חלון לפי מחלקה וגודל.
7. הקלקה על חלון שאינו בחזית נבלעת בהפעלה — למקד קודם, ואז להקליק.
8. בדיקות שמחפשות קובץ פלט עם שם עברי — לוודא BOM בסקריפט, אחרת ההשוואה נכשלת בשקט.
9. **הרצה של `powershell -File test-x.ps1 | Out-String` יכולה להיתקע אחרי שהבדיקה כבר נגמרה.**
   תהליך ffmpeg שהבדיקה הפעילה (למשל צילום פריים) יורש את ערוץ השגיאות של הבדיקה.
   אם הוא נשאר חי, הצינור לא נסגר והלולאה מחכה לנצח. זה קרה ב-`test-ui`, והתוצאה הייתה 45/45.
   לאתר עם `Get-CimInstance Win32_Process -Filter "Name='ffmpeg.exe'"` ולסגור.

---

## 4. מבנה הקוד

| קובץ | אחריות |
|---|---|
| `Program.cs` | `Main`, מקדם DPI (`Theme.Scale`), `Settings` (‏ini ב-`%APPDATA%`), `AppIcon` |
| `Runtime.cs` | פריסת ffmpeg מהמשאב אל `%LOCALAPPDATA%\SubtitleStudio\runtime`, `SplashForm` |
| `Updater.cs` | `App.Version`/`App.Repo`, בדיקת עדכון מ-GitHub, החלפת EXE, `AboutDlg`, `Updates` |
| `MainForm.cs` | החלון הראשי: סרגל, כרטיסים, פריסה, קיצורים, כל פעולות המשתמש |
| `Hero.cs` | מסך הפתיחה (אזור גרירה, שלושה שלבים, קבצים אחרונים, קישורי עזרה למעלה) |
| `Theme.cs` | צבעים, גופנים, `S()` לסקאלה, `Str()` לציור טקסט, `Ltr()` לטקסט טכני |
| `Icons.cs` | כל האייקונים מצוירים בקוד (‏`Ico` enum) — אין גופן אייקונים ואין קבצים |
| `UiKit.cs` | `SurfaceControl` (שקיפות אמיתית), `Btn`, `Card`, `Toggle`, `Slider`, `Field`, `Combo`, `Lbl`, `PopupMenu`, `ScrollHost`, `Ui.Msg`, הרמזים |
| `Surface.cs` | ‏`Tween`/`Anim` (שעון אחד לכל התנועה), משטחים (`Raised`, `Accent`, `Inset`, `Rim`), `ProgressBar`, טבעת מיקוד |
| `Lang.cs` / `LangEn.cs` | שפת הממשק: ‏`Lang.T(עברית)`. ‏**`LangEn.cs` נוצר** מ-`build\lang\en.tsv` על ידי `make-lang.ps1` |
| `Timeline.cs` | ציר הזמן: פס קול, בלוקים, גרירה, סימון טווח, זום |
| `CueList.cs` | רשימת הכתוביות (מצוירת ידנית, גלילה מותאמת) |
| `VideoPreview.cs` | תצוגת הפריים + ציור הכתובית מעליו |
| `Model.cs` | `Cue`, `Doc` (‏ביטול/חזרה, מיון, חיתוך טווח), `Tc` (זמנים) |
| `Formats.cs` | קריאה/כתיבה SRT/VTT/ASS/SUB, זיהוי קידוד, יבוא טקסט חופשי, תרגום |
| `Style.cs` | `SubStyle` — עיצוב הכתובית, ציור תצוגה מקדימה, ייצוא שורת ASS |
| `Player.cs` | `Waveform`, `AudioOut` (waveOut), `VideoPipe`, `FrameGrabber`, `Engine` |
| `Ff.cs` | איתור/הרצת ffmpeg, `ProbeFile`, עבודות עם התקדמות ורצף שלבים |
| `Dlg.cs` | בסיס לכל הדיאלוגים + `ProgressDlg` |
| `DlgExport.cs` | הטמעה בסרט (צריבה/ערוץ) + חיתוך קטע |
| `DlgTools.cs` | `MediaTool`/`MediaTools` (17 כלים), `ToolsDlg`, `ToolRunDlg`, `FitPlan` |
| `DlgEdit.cs` | הזזת תזמון, סנכרון, יבוא טקסט, שליפת כתוביות, עיצוב |
| `Qa.cs` | בדיקת שגיאות: הכללים, ההסברים, והתיקון האוטומטי |
| `SpellEngine.cs` | בודק האיות: קורא את מילון Hspell, תחיליות, ארמית, והצעות. עצמאי, בלי `Theme` |
| `Spell.cs` | מנהל האיות: הורדת המילון, טעינה ברקע, מילון אישי, ומה ש-`Qa` שואל |
| `DlgSpell.cs` | חלון ההורדה של המילון |
| `Native.cs` | Win32: DWM (כותרת כהה/פינות), waveOut, EM_SETCUEBANNER |
| `Fonts.cs` | טעינת גופן הממשק המוטמע (IBM Plex Sans Hebrew מ-0.8.1; עד אז Assistant) — **גם** PrivateFontCollection (GDI+) **וגם** AddFontMemResourceEx (GDI) |
| `Ai.cs` | חיבור ל-Gemini: JSON, קריאות פונקציה, תרגום, והצפנת המפתח (DPAPI) |
| `DlgAi.cs` | הסבר והזנת מפתח, חלון תרגום, וחלון התקדמות |
| `DlgChat.cs` | חלון הצ'אט (ללא מסגרת) + `IAiHost` |
| `AiHost.cs` | הצד של התוכנה: אילו פעולות ה-AI רשאי להפעיל ואיך |
| `Transcribe.cs` | תמלול אוטומטי: חיתוך לקטעים, ויסות מול המכסה, ואיחוי |
| `Stt.cs` | ספקי התמלול: `ISttProvider`, גוגל, ו-Groq (כל שירות תואם OpenAI) |
| `ErrorText.cs` | תרגום כשלים של ffmpeg ושל קבצים להודעה אנושית אחת |
| `Project.cs` | קובץ הפרויקט `.subtext`: כתיבה אטומית, קריאה, נתיבים יחסיים |
| `DlgTranscribe.cs` | בחירת השירות וההסכמה לשליחת הקול, החיבור ל-Groq, וחלון ההתקדמות |

---

## 4א. עקרונות המוצר — המבחן לכל פיצ׳ר

נוסחו על ידי בנימין בפורום, ומצוטטים כלשונם:

> אני אשמח להוסיף כל פיצ'ר רלוונטי ב-2 תנאים; **שלא יגרע מהפשטות
> של התוכנה, לא להבריח טכנופובים**, ו**שלא יגדיל את משקל התוכנה**.

שני מישורי ההתקדמות, גם הם משם: **הפקודות המקומיות אופליין**,
ו**התמלול, התרגום והפעולות הנוספות — אונליין**.

**״משקל התוכנה״ = משקל ה-EXE שמורידים.** מודל או מילון שיורדים
בנפרד ולפי בחירה אינם מגדילים אותו — בדיוק כמו מנוע הווידאו.

לפני שמוסיפים משהו: **`docs/ROADMAP.md` שלב 0** מכיל את טבלת
המבחן, ונספח ד׳ מפרט מה **נבדק ונדחה** — כדי שלא ייבדק שוב.

---

## 5. מוסכמות ממשק — חובה לשמור עליהן

### סקאלה
כל מידה קבועה עוברת דרך **`Theme.S(v)`**. המסך כאן ב-125% והגופנים גדלים לבד;
מספר קשיח בפיקסלים = טקסט חתוך. אין יוצא מן הכלל.

### טקסט
- ציור טקסט **תמיד** דרך `Theme.Str()`. היא מרחיבה מלבן נמוך מגובה הגופן
  (‏GDI+ פשוט לא מצייר אחרת), ומשתמשת ב-`TextRenderer` לטקסט אטום — חד יותר ומטפל
  נכון ברווחים בעברית (‏`GenericTypographic` בולע רווח באמצע מחרוזת RTL).
- **`Theme.Ltr(s)`** לכל מחרוזת טכנית בתוך טקסט עברי: נתיבים, רזולוציה, `+0.5`, `185 MB`.
  בלי זה הסדר מתהפך (`0.5+`, `MB 185`).

### פקדים
- **אסור `BackColor = Color.Transparent`** על פקד שההורה שלו מצייר בעצמו — WinForms
  מריץ מחדש את ה-Paint של ההורה בתוך הבן, והכותרת משוכפלת לתוך כל פקד.
- מלבן מעוגל ברוחב/גובה אפס מפיל את GDI+. `Theme.RoundRect/FillRound/DrawRound`
  כבר מגנים על זה — לא לעקוף אותם.
- קיצורי מקלדת רק ב-**`ProcessCmdKey`** של `MainForm`. `KeyDown` לא נורה כשהמיקוד
  על פקד ש-`IsInputKey` שלו מחזיר false.

### דיאלוגים
- יורשים מ-`Dlg`; שורות נוספות עם `Row(control, height, gap)` (המידות **לוגיות**, `Row` מכפיל בסקאלה).
- `Buttons(ok, icon, cancel)` סוגר את החלון ומחשב גובה. `BottomButton()` להוספת כפתור נוסף.
- הסתרה/הצגה של שורות → לקרוא ל-**`Restack()`** אחרי השינוי, אחרת נשארים חורים.
  `Restack` קורא את דגל הנראות הפנימי ב-reflection, כי `Visible` מחזיר false כל עוד החלון לא הוצג.

### עיצוב
- מסך הפתיחה: **בלי סרגל כלים ובלי שורת מצב** — רק אזור גרירה, שלושה שלבים, קבצים אחרונים,
  ושלושה קישורים בפינה השמאלית העליונה.
- מסך העבודה: 4 כפתורים בסרגל — **פתיחה · שמירה · כתוביות ▾ · הסרט ▾** — ומשמאל
  הכפתור הראשי (יצירת סרט עם כתוביות), ביטול/חזרה ושלושה מתגי אייקון (עזרה, כהה/בהיר, על התוכנה).
- **הפרדת תחומים היא כלל, לא העדפה:** כל מה שנוגע לכתוביות בתפריט ״כתוביות״ (עם כותרות פנימיות:
  הבאת כתוביות · תזמון · תרגום · מראה), וכל מה שנוגע לקובץ הווידאו בתפריט ״הסרט״.
  ערבוב של השניים הוא באג. `MenuItem.Group("...")` יוצר כותרת קטע בתפריט.
- **תוויות קצרות.** הסרגל מתקפל לאייקונים בלבד כשאין מקום (`compact` ב-`DoLayout`), וזה נראה
  כמו ממשק לא מובן. במסך 1493px ב-125% זה קרה עם תוויות ארוכות — לכן ״פתיחה״ ולא ״פתיחת קובץ״.
  אחרי כל הוספת כפתור לסרגל — לצלם ולוודא שהתוויות עדיין מוצגות.
- כפתור שפותח תפריט מקבל `Menu = true` (חץ קטן) כדי שייקרא כתפריט.
- הכלל הכללי: מינימום פקדים גלויים, מקסימום יכולות מאחורי שני תפריטים ברורים.

---

## 6. מנוע המדיה

- **רק `ffmpeg.exe`. אין `ffprobe`** (חוסך 185MB). כל המידע מגיע מניתוח הפלט של
  `ffmpeg -i` ב-`Ff.ParseFfmpegInfo`, ואיתור פריים מפתח דרך
  `select=eq(pict_type\,I),showinfo`.
- הבילד המצורף הוא ה-full build של gyan.dev, **כולל `libass+fribidi+harfbuzz`** —
  זה מה שמאפשר צריבת עברית RTL נכונה. בילד בלי fribidi ישבור את העברית.
- צריבה: כותבים ASS זמני בשם קצר באנגלית ל-`%TEMP%\SubStudio` ומריצים עם
  `WorkingDirectory` שם, עם `-vf subtitles=<name>.ass`. **לא** להעביר נתיב מלא לפילטר —
  נקודתיים ובקסלשים שוברים אותו.
- עבודות מרובות שלבים (קידוד דו-מעברי): `FfJob.Steps` + `StepNames`.

---

## 7. פריסה ועדכונים

- ה-EXE נושא את ffmpeg דחוס. בהפעלה ראשונה `Runtime.Prepare()` פורס אותו
  (‏~2.5 שניות, עם חלון התקדמות) אל `%LOCALAPPDATA%\SubtitleStudio\runtime\ffmpeg.exe`.
- סדר חיפוש: לצד ה-EXE → `tools\` → `runtime\` → תיקיית המשתמש → `PATH`.
- `portable.txt` לצד ה-EXE = פריסה ל-`runtime\` שלידו (דיסק-און-קי).
- אם בכונן המערכת פחות מ-800MB פנויים — נפרס לצד ה-EXE.
- עדכונים: `Updates.CheckSilent` בכל הפעלה (ניתן לכיבוי), API של GitHub Releases,
  הורדה + סקריפט cmd שמחליף את הקובץ ומפעיל מחדש.

---

## 8. מצב נוכחי

> **העבודה אחרי 0.8.0 מתוכננת ב-[`docs/WORKPLAN.md`](docs/WORKPLAN.md). לקרוא אותו
> קודם.** שני מישורים: איכות חזותית ברמה בינלאומית (בעדיפות, ביקש בנימין ב-23.9),
> וממשק באנגלית (0.8.1, התשתית עובדת).

### עובד ונבדק
- יצירה/עריכה/תזמון של כתוביות, ציר זמן עם פס קול, ניגון מסונכרן (‏waveOut + זרם פריימים)
- יבוא SRT/VTT/ASS/SUB/טקסט, זיהוי קידוד כולל Windows-1255
- צריבה בתמונה (עברית RTL נכונה — נבדק על פריים אמיתי), ערוץ כתוביות נפרד, חילוץ כתוביות
- חיתוך (מהיר/מדויק) עם אזהרת פריים מפתח אמיתית, 17 כלי מדיה, קידוד דו-מעברי לגודל יעד
- פריסה מ-EXE יחיד, מצב נייד, ערכה כהה ובהירה, כל הדיאלוגים נבדקו ויזואלית

### 0.8.0 — Subtext (שוחרר 23.9.2026)

**לקרוא קודם: `docs/REVIEW-0.8.md`** — הסקירה שבנימין ביקש לפני הגרסה העגולה.

**ההחלטות של בנימין (19.9.2026):**
- **שם המאגר:** ״שיקול דעת מקצועי, המיתוג חשוב״. **ההחלטה:** 0.8.0 מקבלת הורדות משני השמות (`Updater.RepoNames`, כבר בקוד), ויוצאת כשהמאגר עדיין `subtitle-studio`. **משנים את שם המאגר רק אחרי שרוב המשתמשים על 0.8.x**, ואז 0.8.x הבאה מעדכנת את `App.Repo`. 0.7.x ומטה מקבלות רק את השם הישן: שינוי מוקדם מנתק אותן.
- **עומק:** **רק מה שהמשתמש רואה.** תיקיות `%AppData%\SubtitleStudio`, ‏`%LocalAppData%\SubtitleStudio`, מפתחות הרישום, ה-namespace בקוד — **נשארים.** בלי הגירה.
- **הקיצור:** ״Subtext״ בלבד (לא ״Subtext · אולפן הכתוביות״). בתוך התוכנה, ליד השם, ״אולפן הכתוביות״.
- **העוזר:** ממשיכים לפתח. לכל פיצ׳ר — כלי. **חסרים לו כלים לאיות** (מ-0.7.3).

**נעשה ונכנס לקומיט (19.9.2026):**
1. `2c9e590` שמירה לא הורסת מקור (‏`.sub`/`.txt` ← ״שמירה בשם״ SRT; `Formats.CanSaveInPlace`), כתיבה בטוחה (`SafeFile`), צירוף לא משנה את `FilePath`, **גיבוי לכל חלון עם מנעול** (`session-<pid>-<n>.subtext` + `.lock`, `PendingRecovery`/`ResolveRecovery`). ‏`MainForm.SavePathPicker` לבדיקות. ‏`test-project` 105.
2. `30faab3` הורדות נבדקות (`Updater.Fetch`: אורך, גודל מה-API, ‏SHA-256 מ-`digest`), הודעת סינון (`ErrorText.Filtered`, ‏`IsWebPage`, ‏`Web`), גשר לשם המאגר. ‏`test-release` 42, ‏`test-spell` 47.
3. `65a1b9e` חיתוך לא כותב על המקור. `458c8d5` הסרה מוחקת גם את המילון. `ce76626` קוד מת (סקריפט העדכון הישן), ‏`test-logic` 175.

**נעשה ב-22.9.2026:**
4. **חלון הגדרות חדש** (`DlgSettings.cs`): מראה · **שירותים באינטרנט** (שני המפתחות ככרטיסים, `AiSetupDlg` ו-`GroqSetupDlg`) · בדיקת איות (מתג + הורדת המילון) · עדכונים · **אחסון** (מנוע ומילון: גודל ומחיקה, `Spell.SizeOnDisk`/`Spell.Remove`) · איפוס בכפתור תחתון. **674 לוגי במצב הגבוה ביותר**, מתחת לתקרה של 693, ועם מקום לבורר השפה של 0.8.1 בשורת ״מראה״. ״הגדרות ה-AI״ ירד מתפריט הכתוביות.
5. **כלים לעוזר:** `check_spelling`, ‏`fix_spelling`, ‏`add_word_to_dictionary`. ההנחיה אומרת לו **לא לתקן לבד** אלא להציג ולבקש אישור.
6. **השם**, בצד שהמשתמש רואה: כותרת החלון, מסך הפתיחה (``Sub``+``text`` בשני צבעים, לטיני ולכן משמאל לימין), ״על התוכנה״, `AssemblyInfo.cs`, ‏`dist\Subtext.exe`, ‏`Subtext-Setup.exe`, הקיצור ״Subtext״, והלוגו. **המתקין מוחק את `SubtitleStudio.exe` ואת ״אולפן הכתוביות.lnk״** (`RemoveLegacy`), מזהה את שני שמות התהליך, ומתקין לתיקייה הקיימת מהרישום אם יש (‏`Prod.DefaultDir`) - **בלי קוד הגירה**. תיקיות ההגדרות והרישום נשארו `SubtitleStudio`.
7. **התיעוד:** README, ‏INSTALL, ‏THIRD-PARTY, ‏CHANGELOG, ‏changelog.json, והפיצול ל-`docs/HISTORY.md` (‏1,667 שורות ← 336).

**השחרור:** https://github.com/BeniaBot/subtitle-studio/releases/tag/v0.8.0

**רשימת השחרור:** 20 חבילות, **819 בדיקות**, כולן ירוקות על הבנייה הסופית. ‏`test-release` (42) נכנסה לרשימה — היא בודקת את מנגנון העדכון ולא הייתה בה.

**בנימין ביקש סריקה לפני השחרור (״גם UI, גם UX, גם כל דבר נוסף״), והמהדורה הוחזרה זמנית ל״טרום הפצה״ עד שהסתיימה** — כך שאף משתמש לא קיבל הצעת עדכון בינתיים. מה שנבדק ביד, מעבר לבדיקות:

| מה | תוצאה |
|---|---|
| **שדרוג של עותק מותקן** — מסלול שלא נבדק מעולם | עבר: `SubtitleStudio.exe` נמחק, ‏`Subtext.lnk` החליף את ״אולפן הכתוביות.lnk״, הרישום ``Subtext 0.8.0``, והתוכנה עלתה לבד |
| שדרוג של עותק נייד (`test-upgrade-real -From v0.7.3`) | עבר, והקובץ שהוחלף זהה למהדורה |
| התקנה חדשה, והסרה מלאה | עבר: ההסרה ניקתה קבצים, רישום, קיצור ושיוך; ההגדרות והמנוע נשארו |
| לחיצה כפולה על `.subtext` אחרי השדרוג | נפתח בתוכנה החדשה |
| 21 חלונות × שתי ערכות, צילום וסריקה בעין | תקין |
| מסך העבודה **בערכה בהירה**, בהרצה אמיתית | תקין. אין לזה בדיקה אוטומטית |
| הקובץ שירד מהמהדורה עצמה | רץ, וזהה לבנייה |
| צריבת כתוביות מקצה לקצה (`test-burn-headless`) | 12 בדיקות, עבר |

**באגים שנמצאו רק בעין, ותוקנו:**
- ‏״הקול מהסרט יישלח **ל-גוגל**״. המקף שייך לשם לועזי (``ל-Groq``) ולא למילה עברית. ‏`Theme.Pfx` מחליט לפי האות הראשונה.
- הודעת הסינון החדשה הכילה `github.com` בלי `Ltr`, והכתובת התהפכה בתוך המשפט.
- **צילומי המסך בתיעוד היו מ-0.3 והראו את השם הישן** — זה הדבר הראשון שרואים בדף הפרויקט. רועננו מהתוכנה האמיתית.
- ‏README אמר ״הקובץ שוקל 36 מגה״; הוא 38.

**מלכודת בבדיקת השדרוג:** הגרסה ש**מורידה** היא הישנה, והיא שומרת את הקובץ הזמני בשם שלה (`SubtitleStudio-<גרסה>.exe` עד 0.7.3). הבדיקה עקבה רק אחרי הדפוס החדש, ״הקובץ לא גדל״ עצר את ההמתנה אחרי דקה, והיא הכריזה על כישלון בזמן שההורדה רצה. עכשיו היא עוקבת אחרי שני הדפוסים.

**מה שנשאר פתוח:**
1. **אחרי שרוב המשתמשים יעברו ל-0.8.x:** לשנות את שם המאגר, ואז לעדכן את `App.Repo`. ‏`Updater.RepoNames` כבר מקבל את שני השמות.
2. **פתיחת פרויקט לא בוחרת כתובית** — הרשימה מדגישה את מה שמתנגן, והעורך למטה נשאר ריק. שווה לשקול ל-0.8.1: לשמור בפרויקט איזו כתובית נבחרה, ולחזור אליה.
3. קיצור שהמשתמש **הצמיד לשורת המשימות** מצביע לקובץ הישן ונשבר. נכתב בתיאור המהדורה; תיקון אמיתי יקר.

**ההיסטוריה המלאה - כל גרסה, מה נשבר בה ולמה - עברה ל-[docs/HISTORY.md](docs/HISTORY.md).**
כאן נשאר רק מה שעדיין נכון: הכללים, המלכודות, ורשימת השחרור.

### רשימת שחרור — חובה, בכל גרסה

מ-0.7.1. כל שורה כאן נולדה מבאג שיצא, או כמעט יצא:

1. **כל החבילות, במספר הבדיקות המלא - `build	est-all.ps1`:** logic, autotime,
   scenecuts, list, roundtrip, update-notes, ui, layout, project, screens, fuzz,
   long, stt, errors, buttons, source, qa, spell, release, settings.
   **ירוק עם פחות בדיקות הוא לא ירוק.** ‏`project` דורש מתקין בנוי
   (`make-installer.cmd`).
2. **`test-real-open.ps1`, ולהסתכל על הצילום.** זו הבדיקה היחידה שמריצה
   את קובץ ההפעלה האמיתי.
3. `App.Version`, רשומה ב-`changelog.json`, ו-`CHANGELOG.md`.
4. בנייה, מתקין, תגית, ו-`gh release create` עם שני קובצי ה-EXE.
   **בגוף המהדורה: בלי HTML ובלי גדרות קוד.** גרסאות 0.7.0 ומטה מציגות
   אותם כמו שהם.
5. `test-update.ps1` מול המהדורה החיה, ובדיקת התקציר מהגרסה הקודמת
   (`Changelog.Fetch`).
6. **`test-upgrade-real.ps1 -From v<הגרסה הקודמת>`:** שדרוג אמיתי דרך
   מנגנון העדכון. הסקריפט בונה את התגית הקודמת, מריץ אותה כגרסה ניידת,
   לוחץ ״לעדכן עכשיו״, ומוודא שהקובץ שהוחלף זהה ל-`dist`. **להסתכל על
   `D:\Claude\_ss-upgrade\shots\1-offer.png`**, שבו רואים את התקציר
   שהמשתמש יקבל, **וגם על `1b-after-click-*.png`**. בצילומים האלה, של
   חלון ההורדה בגרסה הישנה, נמצא ״MB 37״ ההפוך.
   - **הבדיקה נכשלת בקוד יציאה** על כל שורת ״!!״. עד 15.9.2026 היא יצאה
     בקוד 0 גם אחרי ״no new instance was started״.
   - **אין תקרה קבועה להורדה.** מחכים כל עוד הקובץ גדל בדקה האחרונה, עד 20
     דקות. תקרה של 180 שניות הרגה את התוכנה באמצע, בלילה איטי של 4MB לדקה.
   - **0.7.1 ל-0.7.2 עבר (15.9.2026):** ההצעה אחרי 3 שניות, הורדה של 52
     שניות, הקובץ זהה לשחרור, הגרסה החדשה עלתה, לא הציעה שוב, ונסגרה נקי.

### לגרסה הבאה

**התוכנית המלאה נמצאת ב-`docs/ROADMAP.md`** — קובץ פרויקט, ספקי STT
חלופיים (‏Groq נותן 8 שעות אודיו ביום חינם מול ~20 בקשות אצל גוגל),
תמלול מקומי, ומיתוג. **לקרוא אותו לפני שמתחילים משהו גדול.**

**דרישה תפעולית חדשה מ-0.6.4: לעדכן את `changelog.json` בכל שחרור.**
בלעדיו המשתמש מקבל תקציר עדכון חסר, והכשל שקט.

- **OCR לכתוביות תמונה (PGS/VobSub) — נבדק ונדחה.** ‏Windows מביא מנוע OCR
  מובנה (`Windows.Media.Ocr`) שאפשר להגיע אליו גם מ-‎.NET Framework דרך
  `/reference:` ל-winmd. **אבל** `OcrEngine.AvailableRecognizerLanguages`
  במכונה הזאת מחזיר `en-US` בלבד, ועברית איננה ברשימת השפות שמיקרוסופט
  תומכת בהן ב-OCR כלל. כלומר: עבודה לא קטנה, שלא תיתן כלום לקהל היעד.
  אם בכל זאת — Tesseract חיצוני, לא מוטמע.
- **חתימת קוד** — ווינדוס מזהיר בכל הפעלה. תעודה עולה כסף. בינתיים כדאי
  לפרסם SHA-256 של כל מהדורה כדי שאפשר יהיה לוודא שהקובץ לא שונה.
- **`docs/screenshots/burned-frame.png`** לא רוענן; הוא לא תלוי בממשק.

### ידוע ופתוח
- כתוביות תמונה (PGS/VobSub) — חילוץ בלבד, אין OCR

### פורסם
**https://github.com/BeniaBot/subtitle-studio** — מאגר ציבורי, תגית `v0.1.0`,
מהדורה עם `SubtitleStudio.exe` (72.8MB) כנכס להורדה.
מנגנון העדכון נבדק מול המהדורה החיה (`build\test-update.ps1`) ✅

**לגרסה הבאה:** להעלות את `App.Version`, לבנות, `git tag vX.Y.Z`,
`gh release create` עם ה-EXE. המשתמשים יקבלו הצעת עדכון בהפעלה הבאה.

**קומיטים עם `237136058+BeniaBot@users.noreply.github.com`** (הכתובת ב-gmail לא מאומתת)

### רישוי — לשים לב
ה-EXE מכיל בינארי של FFmpeg תחת **GPLv3**. הקוד שלנו MIT, אבל ההפצה של ה-EXE
כפופה ל-GPLv3, ולכן `THIRD-PARTY.md` חייב להתלוות למהדורה (קרדיט, גרסת הבילד,
וקישור לקוד המקור). ffmpeg מורץ כתהליך נפרד — אין קישור קוד.

---

## 9. עבודה במקביל בכמה סשנים

- **לתעד כאן כל שינוי מיד בסיומו** — זה הקובץ שממנו סשן אחר מתחיל.
- לא להריץ שתי בניות במקביל: `dist\Subtext.exe` ננעל.
- לפני בנייה: `Get-Process Subtext | Stop-Process -Force`.
- קבצי בדיקה זמניים נכתבים ל-scratchpad של הסשן, לא לתיקיית הפרויקט.
